server:
  listen: 0.0.0.0:4000
  cors:
    origins:
      - https://studio.apollographql.com
telemetry:
  tracing:
    otlp:
      endpoint: collector:4317
      protocol: grpc
      # TODO: This needs to be a "duration"
      # timeout: 42
